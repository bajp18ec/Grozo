<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/app.css">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@300&display=swap" rel="stylesheet">
    <title>Document</title>
</head>
<body>
    <%- include('../partials/nav.ejs') %> 
    <section class="cart py-16">

        <% if(session.cart) {%> 
        <div class="order container mx-auto w-1/2">
            <div class="flex item-center border-b brorder-gray-300 pb-4">
                <img src="/img/cart-black.png" alt="">
                <h1 class="font-bold ml-4 text-2xl">Oreder sumary</h1>
            </div>

            <div class="cart-list">
                 
                <% for(let cartitem of Object.values(session.cart.items)) {  %> 
                    
                <div class="flex items-center my-8">
                    <img src="/img/<%= cartitem.item.image %> " alt="" class="w-24">

                    <div class=" flex-1 ml-4">
                        <h1 class="font-bold text-xl"><%= cartitem.item.name %></h1>
                    </div>
                    <span class="flex-1"><%= cartitem.qty %>pcs</span>
                    <span class="font-bold "><%= cartitem.item.price * cartitem.qty %></span>
                </div>

               <% }  %> 

            </div>
            <hr>
            <div class="text-right py-4">
                <div >
                    <span class="text-lg font-bold">Total Ammount: </span>
                    <span class="amount text-2xl font-bold ml-4"><%= session.cart.totalPrice %> </span>
                </div>

                <div>
                    <form action="" class="mt-12">
                        <input type="text" class="border border-gray-400 p-2 w-1/2 mb-4" placeholder="Phone Number">

                        <input type="text" class="border border-gray-400 p-2 w-1/2" placeholder="Address">
                        <div>
                            <button type="submit" class="btn-primary px-6 py-2 rounded-full text-white mt-6">Order Now</button>
                        </div>
                    </form>
                </div>
                <a href="" class="inline-block coursor-pointer px-6 
                btn-primary px-6 py-2 rounded-full text-white mt-6">Login to continue</a>
            </div>
        </div>
        <% } else { %> 

        <div class="empty-cart py-16">
            <div class="container mx-auto text-center">
                <h1 class="text-3xl fond bold mb-2">Cart Empty </h1>
                <p class="text-gray-800 text-lg mb-12">you probably haven't Oreder any item yet.<br>
                    to order go to main page</p>
                <img src="/img/empty-cart.png" alt="empty-cart" class="w-2/5 mx-auto">
                <a href="/" class="inline-block px-6 py-2 rounded-full btn-primary txt-white mt-12">Go back</a>
            </div>
        </div>
        <% } %> 
    </section>

</body>
</html>